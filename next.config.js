/** @type {import('next').NextConfig} */
const nextConfig = {
  distDir: './dist',
  images: {
    unoptimized: true,
  },
  // experimental: {
  //   appDir: true,
  // },
};

export default nextConfig;
